export function inspectViewHtml(nonce: string): string {
  return `<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'nonce-${nonce}'; script-src 'nonce-${nonce}';"><style nonce="${nonce}">*{box-sizing:border-box}body{margin:0;color:var(--vscode-foreground);background:var(--vscode-sideBar-background);font:13px var(--vscode-font-family)}section{padding:10px 12px 12px;border-bottom:1px solid var(--vscode-sideBarSectionHeader-border,var(--vscode-panel-border))}h2{margin:0 0 7px;color:var(--vscode-sideBarSectionHeader-foreground,var(--vscode-foreground));font-size:11px;font-weight:700;text-transform:uppercase}p{margin:0;color:var(--vscode-foreground);line-height:1.45}.muted{color:var(--vscode-descriptionForeground)}ul{margin:7px 0 0;padding-left:22px;line-height:1.5}button{border:0;border-radius:2px;padding:0;color:var(--vscode-textLink-foreground);background:transparent;font:inherit;cursor:pointer}button:hover{color:var(--vscode-textLink-activeForeground);text-decoration:underline}button:focus-visible{outline:1px solid var(--vscode-focusBorder);outline-offset:1px}.primary{width:100%;margin-top:9px;padding:6px 10px;color:var(--vscode-button-foreground);background:var(--vscode-button-background)}.primary:hover{background:var(--vscode-button-hoverBackground)}</style></head><body><section><h2>Inspect</h2><p>Rich details for commits and stashes are shown as you navigate:</p><ul><li>lines in the text editor</li><li>commits in the <button id="graph">Commit Graph</button></li><li>files in <button id="history">Visual File History</button></li><li>stashes in the Home view</li></ul></section><section><h2>Line History</h2><p class="muted">Open a file to inspect its line-level Git history.</p><button id="file-history" class="primary">Open File History</button></section><section><h2>Visual File History</h2><p class="muted">Explore a file, folder, or repository through its commits and change magnitude.</p><button id="repository-history" class="primary">Open Visual History</button></section><section><h2>Search &amp; Compare</h2><p class="muted">Search commits and compare branches, tags, or arbitrary refs in the Commit Graph.</p><button id="compare" class="primary">Open Commit Graph</button></section><script nonce="${nonce}">const vscode=acquireVsCodeApi();for(const id of ['graph','compare'])document.getElementById(id).addEventListener('click',()=>vscode.postMessage({type:'openGraph'}));document.getElementById('history').addEventListener('click',()=>vscode.postMessage({type:'openRepositoryHistory'}));document.getElementById('file-history').addEventListener('click',()=>vscode.postMessage({type:'openFileHistory'}));document.getElementById('repository-history').addEventListener('click',()=>vscode.postMessage({type:'openRepositoryHistory'}));</script></body></html>`;
}
